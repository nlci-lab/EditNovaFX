<Window x:Class="VideoEditor.Views.ScriptureSubtitleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:VideoEditor.ViewModels"
        mc:Ignorable="d"
        Title="Scripture Subtitle Generator" Height="650" Width="950"
        Background="{StaticResource PrimaryBackground}"
        WindowStartupLocation="CenterOwner">
    
    <Window.DataContext>
        <vm:ScriptureSubtitleViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.ColumnSpan="3" Text="Scripture Subtitle Generator" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

        <!-- Left Panel: Selection -->
        <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource SecondaryBackground}" Padding="15" CornerRadius="8">
            <StackPanel>
                <TextBlock Text="Select Content" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AccentColor}" Margin="0,0,0,10"/>
                
                <TextBlock Text="Bible Book:" Foreground="White" Margin="0,5,0,3"/>
                <ComboBox ItemsSource="{Binding Books}" SelectedItem="{Binding SelectedBook}" DisplayMemberPath="Name" Padding="5"/>

                <TextBlock Text="Chapter:" Foreground="White" Margin="0,15,0,3"/>
                <ComboBox ItemsSource="{Binding Chapters}" SelectedItem="{Binding SelectedChapter}" Padding="5"/>
                
                <TextBlock Text="Generation Mode" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AccentColor}" Margin="0,25,0,10"/>
                <RadioButton Content="Verse Mode (Full verse)" IsChecked="{Binding IsVerseMode}" Foreground="White" Margin="0,0,0,5" ToolTip="Each subtitle shows the full verse text"/>
                <RadioButton Content="Segment Mode (Word split)" IsChecked="{Binding IsSegmentMode}" Foreground="White" Margin="0,0,0,5" ToolTip="Splits verse text proportionally by word count"/>
                <RadioButton Content="Phrase Mode (Natural split)" IsChecked="{Binding IsPhraseMode}" Foreground="White" FontWeight="SemiBold" ToolTip="Splits verse text at natural phrase boundaries (Recommended)"/>
                
                <TextBlock Text="Data Sources" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AccentColor}" Margin="0,25,0,10"/>
                
                <TextBlock Text="USFM File:" Foreground="White" Margin="0,5,0,3"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding UsfmPath}" IsReadOnly="True" Padding="5" Background="#333" Foreground="White" BorderThickness="1" BorderBrush="#555"/>
                    <Button Grid.Column="1" Content="..." Command="{Binding UploadUsfmCommand}" Width="30" Margin="5,0,0,0" Style="{StaticResource ModernButton}"/>
                </Grid>

                <TextBlock Text="Timing File:" Foreground="White" Margin="0,15,0,3"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding TimingPath}" IsReadOnly="True" Padding="5" Background="#333" Foreground="White" BorderThickness="1" BorderBrush="#555"/>
                    <Button Grid.Column="1" Content="..." Command="{Binding UploadTimingCommand}" Width="30" Margin="5,0,0,0" Style="{StaticResource ModernButton}"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Right Panel: Preview -->
        <Border Grid.Row="1" Grid.Column="2" Background="{StaticResource SecondaryBackground}" Padding="15" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Subtitle Preview (Editable)" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AccentColor}" Margin="0,0,0,10"/>
                
                <DataGrid Grid.Row="1" ItemsSource="{Binding PreviewSegments}" AutoGenerateColumns="False" 
                          Background="#222" Foreground="White" BorderThickness="0" CanUserAddRows="False"
                          RowBackground="#222" AlternatingRowBackground="#2A2A2A"
                          HorizontalGridLinesBrush="#333" VerticalGridLinesBrush="#333">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#333"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Padding" Value="10,5"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="BorderBrush" Value="#444"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        </Style>
                        <Style x:Key="AdjBtn" TargetType="Button" BasedOn="{StaticResource ModernButton}">
                            <Setter Property="Width" Value="22"/>
                            <Setter Property="Height" Value="22"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Background" Value="#444"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding SegmentId}" Width="50" IsReadOnly="True"/>
                        
                        <!-- Start Time Adjustment -->
                        <DataGridTemplateColumn Header="Start" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="-" Style="{StaticResource AdjBtn}" Margin="0,0,5,0">
                                            <Button.CommandParameter>
                                                <MultiBinding Converter="{StaticResource MultiValueConverter}">
                                                    <Binding Path="."/>
                                                    <Binding Source="-0.1"/>
                                                </MultiBinding>
                                            </Button.CommandParameter>
                                            <Button.Command>
                                                <Binding Path="DataContext.AdjustStartTimeCommand" RelativeSource="{RelativeSource AncestorType=DataGrid}"/>
                                            </Button.Command>
                                        </Button>
                                        <TextBlock Text="{Binding StartTime, StringFormat=\{0:F3\}}" Foreground="#00C1D4" VerticalAlignment="Center" Width="50" TextAlignment="Center" FontWeight="SemiBold"/>
                                        <Button Content="+" Style="{StaticResource AdjBtn}" Margin="5,0,0,0">
                                            <Button.CommandParameter>
                                                <MultiBinding Converter="{StaticResource MultiValueConverter}">
                                                    <Binding Path="."/>
                                                    <Binding Source="0.1"/>
                                                </MultiBinding>
                                            </Button.CommandParameter>
                                            <Button.Command>
                                                <Binding Path="DataContext.AdjustStartTimeCommand" RelativeSource="{RelativeSource AncestorType=DataGrid}"/>
                                            </Button.Command>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- End Time Adjustment -->
                        <DataGridTemplateColumn Header="End" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="-" Style="{StaticResource AdjBtn}" Margin="0,0,5,0">
                                            <Button.CommandParameter>
                                                <MultiBinding Converter="{StaticResource MultiValueConverter}">
                                                    <Binding Path="."/>
                                                    <Binding Source="-0.1"/>
                                                </MultiBinding>
                                            </Button.CommandParameter>
                                            <Button.Command>
                                                <Binding Path="DataContext.AdjustEndTimeCommand" RelativeSource="{RelativeSource AncestorType=DataGrid}"/>
                                            </Button.Command>
                                        </Button>
                                        <TextBlock Text="{Binding EndTime, StringFormat=\{0:F3\}}" Foreground="#00C1D4" VerticalAlignment="Center" Width="50" TextAlignment="Center" FontWeight="SemiBold"/>
                                        <Button Content="+" Style="{StaticResource AdjBtn}" Margin="5,0,0,0">
                                            <Button.CommandParameter>
                                                <MultiBinding Converter="{StaticResource MultiValueConverter}">
                                                    <Binding Path="."/>
                                                    <Binding Source="0.1"/>
                                                </MultiBinding>
                                            </Button.CommandParameter>
                                            <Button.Command>
                                                <Binding Path="DataContext.AdjustEndTimeCommand" RelativeSource="{RelativeSource AncestorType=DataGrid}"/>
                                            </Button.Command>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Text" Binding="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" VerticalAlignment="Bottom" Margin="0,10,0,0">
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#AAA" FontSize="12" Margin="0,0,0,5"/>
                    <ProgressBar Height="4" Value="{Binding Progress}" Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}" Foreground="{StaticResource AccentColor}" Background="#333" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="1. Preview Subtitles" Command="{Binding PreviewSrtCommand}" Width="180" Height="35" FontSize="14" FontWeight="Bold" Style="{StaticResource ModernButton}" IsEnabled="{Binding IsProcessing, Converter={StaticResource InvertBooleanConverter}}" Margin="0,0,10,0"/>
            <Button Content="2. Export SRT" Command="{Binding ExportSrtCommand}" Width="150" Height="35" FontSize="14" FontWeight="Bold" Style="{StaticResource ModernButton}" IsEnabled="{Binding IsProcessing, Converter={StaticResource InvertBooleanConverter}}"/>
        </StackPanel>

    </Grid>
</Window>
